export interface GalleryItem {
  title: string;
  altText: string;
  description: string;
  category: string;
  unit: string;
}
export type UploadType = "profile" | "background" | "document" | "gallery"
